import { getGoalsFromNames } from './goal-factory';

describe('getGoalsFromNames', () => {
  it.skip('utilizes the CMS environment variables and returns the correct implementation', () => {
    getGoalsFromNames(['abc', 'def'], 'Mock');
  });
});
